name: Deploy to Server

on:
  push:
    branches:
      # - main
      # - master
      - test

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          script: |
            if [ ! -d "/root/devops-test" ]; then
              git clone https://github.com/<username>/devops-test.git /root/devops-test
            fi
            cd /root/devops-test
            git pull origin main
            docker compose down
            docker compose up -d --build

